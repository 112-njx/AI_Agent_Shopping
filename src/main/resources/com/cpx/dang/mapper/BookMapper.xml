<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cpx.dangdang.dao.BookDao">

  <select id="selectCommentedBook" resultType="Book">
    select * from t_book order by rand() limit 4
  </select>

  <select id="selectHotSaleBook" resultType="Book">
    select * from t_book order by sale desc limit 4
  </select>

  <select id="selectNewGroundingBook" resultType="Book">
    select * from t_book where create_date >= #{time} order by create_date desc limit 4
  </select>

  <select id="selectNewHotBook" resultType="Book">
    select * from t_book where create_date >= #{time}
    order by create_date desc, sale desc limit 4
  </select>

  <select id="searchBook" resultType="Book">
    select * from t_book
    <where>
      <if test="name!=null">
        book_name like concat('%',#{name},'%')
      </if>
    </where>
  </select>

  <select id="selectBookById" resultType="Book">
    select * from t_book where book_id = #{id}
  </select>

  <select id="selectBookByCategoryId" resultType="Book">
    select * from t_book
    <where>
      <if test="cid!=null">
        category_id = #{cid}
      </if>
      <if test="cid==null">
        category_id in (select category_id from t_category where parent_id = #{pid})
      </if>
    </where>
  </select>

  <update id="updateCount">
    update t_book set stock = #{count} where book_id = #{bookId}
  </update>
</mapper>
